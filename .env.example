# Optional: Override default memory limits (in bytes)
DOCKER_MEMORY_LIMIT=8g
DOCKER_MEMORY_RESERVATION=4g

# Optional: Override default port
STREAMLIT_PORT=8501

# Optional: Force specific architecture (auto-detected by default)
# ARCH=cpu|gpu|arm 